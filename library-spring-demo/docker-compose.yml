version: '3.8'

services:
  app:
    image: openjdk:17-jdk-slim
    container_name: spring_app
    ports:
      - "8080:8080"
    volumes:
      - .:/app
    working_dir: /app
    command: ./gradlew bootRun