apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    io.kompose.service: accountsdb-data
  name: accountsdb-data
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 100Mi
